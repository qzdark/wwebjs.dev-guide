/**
 * Custom Styles here.
 *
 * ref：https://v1.vuepress.vuejs.org/config/#index-styl
 */

/* Root directory for extra properties */
:root {
  --scrollbar-background: #EDEDED;
  --theme-color: #25D366;
}

/* No change due to events */
.home .hero img {
  max-width 450px!important;
}
div.page-nav, footer.page-edit {
  max-width: 1050px;
}
aside.sidebar {
  border-right: 0px;
}
.custom-block.danger, .custom-block.tip, .custom-block.warning {
  border-radius: 6px;
}
a.sidebar-link.active {
  border-radius: 3px;
}
.theme-default-content:not(.custom) img {
  border-radius: 6px;
.theme-default-content:not(.custom) 
  max-width: 1050px;
}
html ::-webkit-scrollbar {
  width: 8px;
}

/* Change Events */
.yuu-theme-dark {
  --scrollbar-background: #171717; 
}
.yuu-theme-blue {
  --theme-color: #2196F3;
}
.yuu-theme-red {
  --theme-color: #DE3636;
}
.yuu-theme-purple {
  --theme-color: #A846EB;
}

/* Changes due to events */
html ::-webkit-scrollbar-track {
  background-color: var(--scrollbar-background);
}
html ::-webkit-scrollbar-thumb {
  background: var(--theme-color);
  border-radius: 10px;
}
html ::-webkit-scrollbar-thumb:hover {
  background: darken(var(--theme-color), 30%);
}