/**
 * Custom Styles here.
 *
 * ref：https://v1.vuepress.vuejs.org/config/#index-styl
 */

:root {
  --scrollbar-background: #EDEDED;
  --scrollbar-hover: #55E189;
  --theme-color: #25D366;
  --custom-block-details: #EDEDED;
}


.yuu-theme-dark {
  --scrollbar-background: #171717;
  .custom-block.details {
    background-color: #323843;
  }
}
.yuu-theme-blue {
  --theme-color: #2196F3;
  --scrollbar-hover: #62B5F7;
}
.yuu-theme-red {
  --theme-color: #DE3636;
  --scrollbar-hover: #E87171;
}
.yuu-theme-purple {
  --theme-color: #A846EB;
  --scrollbar-hover: #C88BF2;
}


.home .hero img {
  max-width 450px!important;
}
div.page-nav, footer.page-edit, .theme-default-content:not(.custom) {
  max-width: 1050px;
}
.custom-block.danger, .custom-block.tip, .custom-block.warning, .custom-block.details {
  border-radius: 6px;
  border-left-width: 0.45rem;
}
a.sidebar-link.active {
  border-radius: 3px;
}
.theme-default-content:not(.custom) img {
  border-radius: 6px;
}
html ::-webkit-scrollbar {
  width: 8px;
}
aside.sidebar {
  border-right: 0px;
}
div[class*="language-"] .highlight-lines .highlighted {
  background-color: #353b45;
}
pre.vue-container
  border-left-width: .5rem;
  border-left-style: solid;
  border-color: #42b983;
  border-radius: 0px;
  & > code
    font-size: 14px !important;
    & > p
      margin: -5px 0 -20px 0;
    code
      background-color: #42b983 !important;
      padding: 3px 5px;
      border-radius: 3px;
      color #000
    em
      color #808080
      font-weight light


html ::-webkit-scrollbar-track {
  background: var(--scrollbar-background);
}
html ::-webkit-scrollbar-thumb {
  background: var(--theme-color);
  border-radius: 10px;
}
html ::-webkit-scrollbar-thumb:hover {
  background: var(--scrollbar-hover);
}
pre .code-copy {
  margin-top -0.85rem;
  svg path:nth-child(2) {
    fill var(--theme-color)
  }
}
button.theme-code-group__nav-tab-active[data-v-deefee04] {
  border-bottom: 1px solid var(--theme-color);
}